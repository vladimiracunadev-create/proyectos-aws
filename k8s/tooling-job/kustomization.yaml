apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tooling-demo

resources:
  - namespace.yaml
  - job.yaml
  - networkpolicy.yaml

commonLabels:
  app: tooling-validate
  managed-by: kustomize
  environment: demo

# ConfiguraciÃ³n comÃºn para todos los recursos
commonAnnotations:
  description: "Demo de validaciÃ³n de tooling en Kubernetes"
  repository: "proyectos-aws"
