version: 1
applications:
  - appRoot: caso-01-amplify-hosting
    frontend:
      phases:
        build:
          commands: []
      artifacts:
        baseDirectory: .
        files:
          - '**/*'
