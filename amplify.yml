version: 1
applications:
  - appRoot: aws-amplify-mi-sitio-1
    frontend:
      phases:
        build:
          commands: []
      artifacts:
        baseDirectory: .
        files:
          - '**/*'
